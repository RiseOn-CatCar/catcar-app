apiVersion: v1
kind: Secret
metadata:
  name: catcar-secret
  labels:
    app.kubernetes.io/name: catcar-api
    app.kubernetes.io/component: api
type: Opaque
# Credentials are materialized from Azure Key Vault by the deployment workflow.
# Production secret synchronization should use the Azure Key Vault provider for the
# Secrets Store CSI Driver. Create this SecretProviderClass after installing the driver:
# apiVersion: secrets-store.csi.x-k8s.io/v1
# kind: SecretProviderClass
# metadata:
#   name: catcar-key-vault
# spec:
#   provider: azure
#   parameters:
#     usePodIdentity: "false"
#     clientID: "<workload-identity-client-id>"
#     keyvaultName: "<key-vault-name>"
#     tenantId: "<azure-tenant-id>"
#     objects: |
#       array:
#         - |
#           objectName: postgres-connection-string
#           objectType: secret
#         - |
#           objectName: jwt-secret
#           objectType: secret
