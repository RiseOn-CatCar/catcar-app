apiVersion: v1
kind: Service
metadata:
  name: catcar-api-service
  labels:
    app.kubernetes.io/name: catcar-api
spec:
  type: LoadBalancer
  selector:
    app: catcar-api
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
